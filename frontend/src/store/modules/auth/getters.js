export const loginResult = (state) => {
    return state.auth;
}


export const routeList = (state) => {
    return state.routelist;
}

export const invalidRoute = (state) => {
    return state.inValidRoute;
}

